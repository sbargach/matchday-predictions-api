CREATE TABLE dbo.Leagues
(
    Id          INT IDENTITY(1,1) NOT NULL CONSTRAINT PK_Leagues PRIMARY KEY,
    Name        NVARCHAR(100) NOT NULL,
    Code        NVARCHAR(20)  NOT NULL CONSTRAINT UX_Leagues_Code UNIQUE,
    Created     DATETIME2(0)  NOT NULL
);
GO
